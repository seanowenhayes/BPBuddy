//

import SwiftUI

struct AddReadingView: View {
    @State private var model = ViewModel(systolic: 105, diastolic: 70, pulse: 80)
    var body: some View {
        Text(/*@START_MENU_TOKEN@*/"Hello, World!"/*@END_MENU_TOKEN@*/)
    }
}

#Preview {
    AddReadingView()
}
